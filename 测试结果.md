# Cavno 下载增量 v3：测试记录

更新时间：2026-09-14。测试对象是本工作区 Cavno 源码的独立副本；未向线上网站部署。

## 已完成

| 检查 | 实际结果 |
| --- | --- |
| Windows 启动脚本 | ASCII 启动文件；PowerShell 语法解析通过；在 Windows PowerShell 中实际执行 Bootstrap 的 check 模式成功，中文正常；Node 运行脚本语法检查通过 |
| 缺少 npm 的首次安装 | 在测试副本内下载官方 npm 11.6.0，完成完整性校验，按锁文件安装 398 个依赖包 |
| 环境检查模式 | 通过，能识别 Node 24.19.0、Edge 和项目依赖；不覆盖源码 |
| 单篇完整流程 | 《几何原本》完成构建、三格式生成、public 审计、第二次构建、dist 审计 |
| 浏览器真实下载 | Edge 测试中实际下载 MD、PDF、DOCX，并验证文件类型 |
| 复制与菜单 | Markdown 正文复制成功；点击展开、Escape 关闭均通过 |
| 响应式抽查 | 1440px 桌面、390px 手机视口；下载菜单未越界，测试文章无横向溢出 |
| 入口范围 | 首页和 Reading 栏目页没有误显示文章下载工具 |
| PDF 抽样结构检查 | 几何原本 13 页、期权图谱 18 页、Claude 订阅风险图谱 13 页；文字提取无 U+FFFD 替换字符，所列字体均嵌入 |
| PDF 视觉抽查 | 检查了代表性页面的中文、图示与分页；没有将其表述为全站逐页验收 |
| 原文缺字处理 | tech-glossary 原始页面含 1 个损坏字符；导出注明“【原文缺字】”，未猜测修改原文 |

浏览器验证共 15 项通过。较早的几次测试中，Edge 对 PDF 下载曾返回 `canceled`；同一源码和链接重试后实际下载通过，未关闭浏览器安全保护，也未改成假 PDF 或伪造扩展名。该偶发现象的根因未确认，不宣称已排除所有浏览器环境问题。

## 全站最终结果

2026-09-14 完成最终版本完整流程，进程退出码为 0：

- 生产构建生成 202 个页面。
- 发现 136 个文章导出入口，生成或校验复用 136 篇，失败 0 篇，共 408 个 MD/PDF/DOCX 文件。
- public 全站审计通过：136 篇、408 个文件。
- 第二次生产构建成功，随后 dist 全站审计通过：136 篇、408 个文件。
- 最终明确输出“【全站生成完成】全站三种下载文件均已生成，并通过最终 dist 检查。”。
- 中断续跑已实测：前 100 篇通过输入签名与文件完整性校验后复用，剩余文章继续生成，并完成全站审计。
- 三种文档总大小约 525.34 MiB，不含额外图示资产；当前最大的下载文件为 five-city-sector-atlas.pdf，23,480,474 字节，约 22.39 MiB。

本机完整日志位于测试副本 `.cavno-tools/logs/2026-09-14T03-16-52-955Z-all.log`，汇总结果在 `.cavno-tools/export-report.json`。这些测试日志不装入代码增量包。

## 使用边界

- 工具只提供增量代码，不附带全部文章的成品下载文件；在自己的最新源码上运行后生成。
- 自动审计检查文档存在、字节数、哈希、UTF-8、Markdown 围栏、PDF 签名和 DOCX 必要结构，不等于每页排版都已人工检查。
- 手机验证是 Edge 手机尺寸视口，不是真机 Safari、微信内置浏览器的全面兼容性测试。
- 未逐一在 Microsoft Word、WPS、Typora、Obsidian 的各版本中打开全站文件。
- 表单、计算器和交互图表导出的是可读文本及当时的静态状态；PDF、Word、Markdown 不保留网页的完整交互能力。
- Markdown 图示引用线上资源；单个 MD 文件不是包含全部图片的离线压缩包。
- 未替用户提交 Git、上传文件或发布 Cloudflare。线上下载还需按操作说明发布生成结果。

请以运行结束时明确的“单篇测试完成”或“全站生成完成”提示判定对应流程成功，不以某一次构建的 `Complete!` 或中途某篇文章的 `OK` 代替整个流程的结果。
