# Cavno 三格式下载：Windows 操作说明 v3

请使用本次 v3 压缩包。旧版的 `Apply-Update.ps1`、`Install-And-Generate.ps1` 和直接输入多条 `npm` 的说明不再是本次入口。

本包是增量源码加运行工具，必须配合现有的 Cavno 网站源码使用。单独把它上传到网站，不会生成全站下载文档。

## 1. 先分清两个文件夹

| 名称 | 里面能直接看到什么 | 用途 |
| --- | --- | --- |
| 网站源码根目录 | `package.json`、`src`、`public`、`scripts` | 程序要更新和构建的网站 |
| 本次增量工具目录 | `Start.cmd`、`Bootstrap.ps1`、`run.mjs`、`payload` | 启动安装、测试和生成操作 |

先把 ZIP 完整解压到普通文件夹，不能在压缩软件里直接运行。`Start.cmd`、`Bootstrap.ps1`、`run.mjs` 和 `payload` 必须保留在同一目录。

找到你正在使用的 Cavno 网站源码，在资源管理器地址栏复制完整路径。路径对应的那一层必须直接包含 `package.json`、`src` 和 `public`。如果只看到另一个 `cavno-site` 文件夹，就继续进入内层。

本次在这台电脑上使用的网站工作目录是：

```text
C:\Users\chowm\Documents\Codex\2026-08-12\da\work\nav-redesign-20260904\cavno-site
```

如果你在别处维护或提交网站，应填那个实际源码路径。不要填网址、RAR 文件、`dist`、`public` 或 `payload` 的路径。

## 2. 第一次先运行一篇文章

1. 打开本次解压出的工具文件夹。
2. 双击 `Start.cmd`。如果资源管理器隐藏扩展名，它可能显示为 `Start`，类型为“Windows 命令脚本”。
3. 窗口要求输入源码目录时，粘贴上一步复制的完整路径，按回车。
4. 看到菜单后输入 `2`，按回车：

```text
1 检查环境
2 单篇测试（首次建议）
3 生成全站
4 打开本地预览
```

脚本随后自动检查环境、备份需要覆盖的文件、安装依赖、构建网站、生成《几何原本》三种文档、再次构建并核对最终文件。第一次可能需要联网下载安装依赖。看到 `npm notice`、版本升级提示或 `deprecated` 警告不等于失败；以最后的成功或失败提示为准，不需要为升级提示操作全局 npm。

成功结束时必须看到：

```text
【单篇测试完成】几何原本文档生成及最终目录检查通过。全站文档尚未生成。
```

只看到 Astro 的 `Complete!`，说明某一次构建完成，不代表整个流程已经结束。请等到上述中文提示出现再关闭窗口。

## 3. 在浏览器里真正验证下载

1. 再次双击 `Start.cmd`。
2. 输入同一个网站源码目录。
3. 菜单输入 `4`，按回车。
4. 保持此窗口打开，在 Edge 或 Chrome 地址栏粘贴：

```text
http://127.0.0.1:4350/reading/thinking/elementsofgeometry/
```

此时你看的是这台电脑刚构建的网站。不要双击 `dist/index.html`，因为网站资源使用以 `/` 开头的路径，需要通过本地网址访问。

在文章右上角依次检查：

| 操作 | 应当看到的结果 |
| --- | --- |
| 点击“下载” | 展开 Markdown、PDF、Microsoft Word 三个选项 |
| 点击 Markdown 文档 | 下载 `.md`，能用 Typora、Obsidian 或记事本打开；中文正常，有标题和正文 |
| 点击 PDF 文档 | 下载 `.pdf`，能用 Edge、Chrome 或 PDF 阅读器打开；正文、图示与分页可读 |
| 点击 Microsoft Word | 下载 `.docx`，能用 Word 打开，不提示文件损坏 |
| 点击“复制正文” | 提示已复制，将内容粘贴到记事本检查是否为 Markdown 正文 |

首次只生成了《几何原本》的文档，其他文章的下载文件尚未准备好。完成下一步“生成全站”后，才检查其他文章的下载。

本地预览窗口关闭后，该本地网址就会失效。这是正常现象。测试结束，在预览窗口按 `Ctrl+C` 停止预览；然后再启动下一步。如果提示端口 `4350` 已被占用，先关闭你之前开的预览窗口，再重试。

Markdown 是文本文件，图示通过网站的完整网址引用。首次仅在本地生成、尚未发布时，Markdown 里的新图示网址可能还无法打开；发布对应的 `downloads/assets` 后才可访问。单独下载 `.md` 不等于把所有图片离线打包。需要直接离线阅读图文时，优先下载 PDF 或 Word。

## 4. 生成整站文章

确认单篇三种文件均能打开后：

1. 停止刚才的本地预览。
2. 再次双击 `Start.cmd`。
3. 输入同一个源码目录。
4. 菜单输入 `3`，按回车。
5. 等待所有文章生成和校验完成。

执行过程中会输出类似：

```text
[1/136] START /investing/cases/creditexpansion/
[1/136] OK /investing/cases/creditexpansion/ ...
```

分母取决于实际源码的文章数，不一定永远是 136。程序可能同时处理两篇，输出顺序不要求按数字递增。`START` 表示开始，`OK` 表示该篇完成，`SKIP 已校验` 表示文件仍然有效，已复用。

全文较长、包含许多图表或外部资源时，一篇需要更长时间；首次整站处理可能需要十几分钟或更久，不能以单篇耗时承诺总耗时。不要在程序工作时同时修改文章、启动另一次构建或再跑另一份生成程序。

本次测试的 408 个下载文档约占 525 MiB。它们会同时出现在 `public` 和 `dist`，加上依赖、原网站、图片和备份，操作前应留出数 GB 可用磁盘空间。增量 ZIP 本身很小，不代表生成后的文档也只有这么大。

成功结束必须看到：

```text
【全站生成完成】全站三种下载文件均已生成，并通过最终 dist 检查。
```

结束后可以再次选择 `4`，随机检查不同栏目的文章下载。

## 5. 生成好的文件在哪里

以《几何原本》为例，源码内将出现：

```text
public\reading\thinking\elementsofgeometry\downloads\
    elementsofgeometry.md
    elementsofgeometry.pdf
    elementsofgeometry.docx
    manifest.json
    assets\
```

最后一次构建会把这些文件复制到 `dist` 对应路径。`assets` 保存文档引用的图示，发布时也要保留。

`public` 是以后每次构建都会复制的文件来源；`dist` 是最终静态网站成品。以后你修改文章，需要重新运行“生成全站”，更新对应下载文档。

## 6. 让线上网站也能下载

本地成功不会自动修改 `https://cavno.org/`。此工具没有自动部署功能。

如果你平时直接上传静态文件，请使用本次最终生成的整个 `dist` 文件夹，包含文章对应的 `downloads` 和 `assets`。

如果你平时通过 GitHub/Git 仓库连接 Cloudflare Pages，请把本次变更的源码、`package.json`、`package-lock.json`，以及 `public` 内生成的所有文章 `downloads` 文件提交到原网站仓库。Cloudflare 继续使用原来的 `npm run build` 构建命令和 `dist` 输出目录。生成器使用本地 Edge/Chrome，因此应在本机生成文档后再提交。

不要提交 `node_modules`、`.cavno-tools`、`_increment-backups` 或本次测试副本。这些分别是本地依赖、工具日志缓存、恢复用备份与测试资料。不要误把受保护的 Working 下载文件移动到网站公共下载目录；它们应保留在原 `/working/` 路径下。

等线上新部署完成，再打开网站文章，按 `Ctrl+F5` 刷新，测试三种下载。只上传源码但漏传 `public/**/downloads/`，是“按钮出现了，但点击 404”的直接原因之一。

## 7. 如果再次失败，按实际提示处理

| 实际提示或现象 | 含义与下一步 |
| --- | --- |
| `run.mjs is missing` 或缺少 payload 文件 | ZIP 没有完整解压，或文件被拆开放置；重新完整解压 v3 |
| 找不到 `package.json` | 源码目录层级错误；选择直接包含该文件、`src`、`public` 的文件夹 |
| 没有符合要求的 Node | 安装 [Node.js 官方下载页](https://nodejs.org/en/download)提供的 Windows 安装程序，选择兼容的 22.12+ 或 24 LTS 版本；安装完成后重新开窗口 |
| `Base.astro` 或 `package.json` 版本不匹配 | 当前源码已有本包之外的修改；保留提示，需要合并这两个实际文件，不要强制覆盖较新版本 |
| 官方 npm 下载失败或安装报网络错误 | 记录完整提示，检查能否连接 `registry.npmjs.org`；网络恢复后重跑相同模式，无需手改全局 registry |
| `EPERM`、`EACCES`、`Access is denied` | 某个目录或文件被占用/权限限制；根据日志中的具体路径检查，先停止之前的构建和预览；不要一律通过删除源码处理 |
| 显示 `FAILED` 和某篇文章路径 | 当前文章的生成失败，其他成功文件已保留；末尾不会报告全站成功，错误清单在 `.cavno-tools/export-report.json` |
| 页面提示复制失败，或者下载 404 | 确认生成过这篇；测试时使用本地预览地址；线上检查是否发布了新的 downloads 文件 |
| 浏览器显示“已取消”或阻止下载 | 打开浏览器下载记录查看具体原因；确认链接不是 404，重试一次。若仍失败，保留下载记录里的错误文字；不要关闭杀毒软件或安全保护来试错 |
| 浏览器无法连接 `127.0.0.1:4350` | 本地预览未启动或窗口已关闭；重新运行工具并选择 `4` |
| 浏览器打开在线域名仍是旧页面 | 先确认新版本已部署完成，再刷新；本地运行不会自动上线 |

工具发生错误时会显示：

```text
【未完成】停在“具体步骤”。
具体错误说明
完整日志：...\.cavno-tools\logs\日期时间-模式.log
```

把该日志文件发给我，或复制从“未完成”开始的错误文字和上方相关报错，即可定位。日志会保留具体失败文章和错误信息。目录选错导致工具尚未建立日志时，直接保留窗口报错即可。

中途停止后，重新选择相同模式。程序会比对文章、样式、生成器和文件校验值后复用有效文件；文章或生成器改变时，重新生成属于正常行为。

## 8. 不方便双击时的备用命令

在工具文件夹的资源管理器地址栏输入 `powershell`，按回车，然后执行下面命令。`-Target` 后请填你的实际网站源码目录：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File ".\Bootstrap.ps1" -Target "C:\Users\chowm\Documents\Codex\2026-08-12\da\work\nav-redesign-20260904\cavno-site" -Mode sample
```

将最后的 `sample` 替换为 `check` 可检查环境，替换为 `all` 可生成全站，替换为 `preview` 可启动预览。此处的执行策略参数只作用于该次子进程，不修改系统的全局设置。

## 9. 本次修正了什么

- Windows 启动文件使用 ASCII，中文提示放在由 Node 明确按 UTF-8 读取的程序中，避免 PowerShell 5.1 编码解析错误。
- 使用项目的 npm 锁文件；找到 Node 后，把其路径传给子进程，解决系统终端没有 npm/Node PATH 的情况。
- 先检查环境和目标版本，再备份应用；所有失败均保存日志。
- 提供单篇测试、全站生成、本地预览和最终 `dist` 校验，成功提示与本说明保持一致。
- 修正不同标题写法导致的 DOCX 误报；打印时移除会显著放大 PDF 的装饰纹理。
- 源文中已经损坏、无法确定的字符在导出时标为“【原文缺字】”并说明来源，没有猜测替换。

实际测试范围和结果请看同目录的 `测试结果.md`。自动文件检查证明能生成和完整下载，不能替代在每种阅读器中逐页查看所有长文的排版。

## 10. 修改范围与恢复

本工具应用的源代码只有 `payload` 内的 7 个文件，不是整站覆盖包。它会先把这些文件的旧版本保存到源码目录的 `_increment-backups/article-downloads-v3-日期时间/`，备份完成后才应用更新。

不过，安装依赖会更新本机 `node_modules`，构建会重新生成 `dist`，文档生成会写入 `public` 对应文章的 `downloads`。七文件备份不等于全站备份。如果这是你唯一一份源码，首次操作前请先在资源管理器中复制一份源码作为保险，不要只备份 `dist`。

要恢复本次覆盖的源码：先停止预览和生成，打开对应日期的备份目录，将其中存在的源文件按相同相对路径复制回网站源码目录。`backup-manifest.json` 记录每个文件更新前是否存在；`existed: false` 表示它是本次新增文件，没有旧版本可恢复。不要把不同日期的备份混用。恢复后如需回到旧网站成品，使用你保留的完整旧版备份重新构建或发布。
