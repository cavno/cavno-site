import { defineConfig } from 'astro/config';

export default defineConfig({
  // 部署后改成实际地址：https://cavno.pages.dev 或自定义域名
  site: 'https://cavno-site.tchow.workers.dev/',
  trailingSlash: 'ignore',
});
